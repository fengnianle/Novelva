import { defineConfig } from 'vite';

// https://vitejs.dev/config
// Note: sql.js, pdfjs-dist, and adm-zip are loaded dynamically via createRequire
// at runtime from the app root, so they don't need to be externalized here.
export default defineConfig({});
